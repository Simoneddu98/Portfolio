# Copia questo file in .env.local e inserisci i valori reali
# MAI committare .env.local su GitHub

# Form contatti (Web3Forms)
WEB3FORMS_ACCESS_KEY=
NEXT_PUBLIC_WEB3FORMS_ACCESS_KEY=
CONTACT_TO_EMAIL=

# WhatsApp
WHATSAPP_ACCESS_TOKEN=

# BOTman chatbot
APPS_SCRIPT_URL=                  # URL webhook Google Apps Script — solo server-side via /api/lead (usa /exec, NON /dev)
GEMINI_API_KEY=                   # ⚠️ MAI aggiungere NEXT_PUBLIC_ — solo server-side via API route
